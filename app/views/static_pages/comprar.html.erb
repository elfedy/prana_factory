<div class="container-fluid">
  <div class="row">

    <div class="col-xs-12 col-md-8 col-md-offset-1">

      <div class="row" id="buy-intro-section">
        <div class="col-xs-12 section-title-container">
          <h1 class="section-title">Elegí lo que más te guste....</h1>
          <p class="section-subtitle">Nosotros te lo llevamos a tu casa.</p>
          <p class="buy-intro-text">A tener en cuenta:</p>
          <ul>
            <li><strong>Sólo</strong> hacemos entregas en <strong>Capital Federal</strong></li>
            <li>Entregamos los días <strong>Lunes, Viernes y Sábados</strong></li>
            <li>El <strong>pedido mínimo</strong> es de <strong>$100</strong></li>
          </ul>
        </div>
      </div>

      <div class="row">
        <div class="col-xs-12">
          <div id="buy-product-list">
            <% @categories.each do |category| %>
              <div class="row buy-category-container">
                <div class="col-xs-12 buy-category-title-container">
                  <h3 class="buy-category-title"><%= category.name %></h3>
                </div>
                <div class="row">
                  <% category.products.each do |product| %>
                    <div class="col-xs-12 col-lg-6 buy-product-container">
                      <div class="row">
                        <div class="col-xs-12 col-sm-4 buy-product-image-container">
                          <%= image_tag(product.image.url(:buy),
                              :class => "buy-product-image") %>
                        </div>
                        <div class="col-xs-12 col-sm-8 buy-product-description-container">
                          <h4 class="buy-product-title"><%= product.name %></h4>
                          <% product.skus.each do |sku| %>
                          <div class="row">
                            <div class="col-xs-6">
                              <p class="buy-product-item">
                                x<%= sku.package.identifier %> - <span class="price">$<%= sku.price %></span>
                              </p>
                            </div>
                            <div class="col-xs-6 text-center">
                              <%= link_to line_items_path(:sku_id => sku.id), method: :post, :remote => true do %>
                                <i class="fa fa-plus-circle add-icon" aria-hidden="true"></i>
                              <% end %>
                            </div>
                          </div>
                          <% end %>
                        </div>
                      </div>
                    </div>
                  <% end %>
                 </div>
              </div>
            <% end %>
          </div>
        </div>
      </div>

    </div>

    <div class="col-xs-12 col-md-3">
      <div class="row" id="order">
        <%= render @order %>
      </div>
    </div>

  </div>
</div>
